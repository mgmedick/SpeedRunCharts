@model SpeedRunApp.Model.ViewModels.ChangePasswordViewModel

@{ ViewBag.Title = "Change Password"; }

@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js"></script>
}

<div>
    <div class="col-sm-auto p-2 align-self-end">
        <h4 class="m-0">Change Password</h4>
    </div>
    <div id="divActivate" class="container mx-auto p-0" style="max-width:500px;">
        @if (Model.IsValid)
        {
            @using (Html.BeginForm("ChangePassword", "SpeedRun"))
            {
                @Html.ValidationSummary()
                <div class="form-group row no-gutters">
                    <label class="col-sm-4 col-form-label">Password</label>
                    <div class="col-sm-auto">
                        @Html.PasswordFor(i => i.Password, new { @autocomplete = "off", @class = "form-control" })
                    </div>
                </div>
                <div class="form-group row no-gutters">
                    <label class="col-sm-4 col-form-label">Confirm Password</label>
                    <div class="col-sm-auto">
                        @Html.PasswordFor(i => i.ConfirmPassword, new { @autocomplete = "off", @class = "form-control" })
                    </div>
                </div>
                <div class="row no-gutters pt-1">
                    <div class="form-group mx-auto">
                        <input id="btnChangePass" type="submit" class="btn btn-primary" value="Submit" />
                    </div>
                </div>
                @Html.HiddenFor(i => i.IsValid);
            }
        }
        else
        {
            <div style="text-align:center;">
                <i class="fa fa-hourglass-end"></i>
                <div>
                    Reset Password link has expired, please <a href="javascript:showResetPass();">Reset Password</a> to try again.
                </div>
            </div>
        }
    </div>
</div>






