@using SpeedRunCommon;
@model SpeedRunApp.Model.ViewModels.SpeedRunViewModel
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration

@section Styles {
    <link rel="stylesheet" href="~/lib/free-jqgrid/css/ui.jqgrid.css" />
    <link rel="stylesheet" href="~/lib/jqueryui/themes/base/jquery-ui.min.css" />
    <link rel="stylesheet" href="~/css/free-jqgrid-custom.css" />
    <link rel="stylesheet" href="~/css/scroller-tab.css" />

    <style>
        .brand-logo {
            width: 25px;
        }
    </style>
}

@section Scripts {
    <script src="@Url.Content("~/js/common.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/js/scroller-tab.js")" type="text/javascript"></script>
}

<div class="container ml-0">
    <div class="form-group row">
        <label for="txtStatus" class="col-sm-1 col-form-label">Status</label>
        <div class="col-sm-11">
        </div>
    </div>
    <div class="form-group row">
        <label for="staticEmail" class="col-sm-1 col-form-label">Email</label>
        <div class="col-sm-11">
            <input type="text" readonly class="form-control-plaintext" id="staticEmail" value="email@example.com">
        </div>
    </div>
    <div class="form-group row">
        <label for="inputPassword" class="col-sm-1 col-form-label">Password</label>
        <div class="col-sm-11">
            <input type="password" class="form-control" id="inputPassword" placeholder="Password">
        </div>
    </div>
    @*<div class="form-group">
            <label for="spnPlayers">Players</label>
            <span id="spnPlayers">
                @Html.CommaSeparated(Model.Players.Select(i => Html.ActionLink(i.Name, "UserDetails", "User", new { userID = i.ID ?? i.Name }).ToHtmlString()))
            </span>
        </div>
        <div class="row no-gutters">
            <div class="form-group">
                <label for="spnRank">Emulated</label>
                <span id="spnRank">@Model.IsEmulatedString</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Date Submitted</label>
                <span id="spnRank">@Model.DateSubmittedString</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Verified Date</label>
                <span id="spnRank">@Model.DateSubmittedString</span>
            </div>
        </div>
        <div class="row no-gutters">
            <div class="form-group">
                <label for="spnRank">Platform</label>
                <span id="spnRank">@Model.Platform</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Category Type</label>
                <span id="spnRank">@Model.CategoryType.Name</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Game</label>
                <span id="spnRank">@Model.Game.Name</span>
            </div>
        </div>
        <div class="row no-gutters">
            <div class="form-group">
                <label for="spnRank">Category</label>
                <span id="spnRank">@Model.Category.Name</span>
            </div>
            <div class="form-group" style="@(Model.Level == null ? "display:none" : "")">
                <label for="spnRank">Level</label>
                <span id="spnRank">@Model.Level?.Name</span>
            </div>
        </div>
        <div class="row no-gutters">
            <div class="form-group">
                <label for="spnRank">Primary Time</label>
                <span id="spnRank">@Model.PrimaryTimeString</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Real Time</label>
                <span id="spnRank">@Model.RealTimeString</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Real Time Without Loads</label>
                <span id="spnRank">@Model.RealTimeWithoutLoadsString</span>
            </div>
            <div class="form-group">
                <label for="spnRank">Game Time</label>
                <span id="spnRank">@Model.GameTimeString</span>
            </div>
        </div>*@
</div>

















